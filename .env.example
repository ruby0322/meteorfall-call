# Most topics in this screen don't need an API key.
# Anything key-shaped detected in git history is an auto-fail (gitleaks).

# Postgres (docker compose up postgres)
POSTGRES_PASSWORD=
POSTGRES_PORT_BIND=

# Backend
DATABASE_URL=
BACKEND_PORT=
FRONTEND_PORT=
BACKEND_INTERNAL_URL=
NEXT_PUBLIC_API_BASE_URL=
